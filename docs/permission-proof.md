# Permission Proof

After running the notebook permission proof cell, capture the output and save screenshot as:

- `docs/permission-proof.png`

Expected evidence:

- Chinese scenario includes events from `chinese_agent`.
- Chinese scenario does **not** include `get_date_tool` usage.
